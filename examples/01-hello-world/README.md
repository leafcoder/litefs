# Hello World 示例

最简单的 Litefs 应用示例，展示了框架的基本用法。

## 功能特性

- 基本的应用创建和配置
- 简单的路由定义
- JSON 响应返回

## 运行方式

```bash
python app.py
```

访问 http://localhost:8080 查看结果。

## 可用路由

- `GET /` - 首页，返回问候信息
- `GET /about` - 关于页面，返回框架信息
