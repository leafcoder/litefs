# ${project_name}

基于 Litefs 框架的 Web 应用

## 快速开始

### 安装依赖

```bash
pip install -r requirements.txt
```

### 启动开发服务器

```bash
python app.py
```

或使用 CLI 命令：

```bash
litefs runserver
```

### 访问应用

打开浏览器访问: http://localhost:9090

## 项目结构

```
${project_name}/
├── app.py              # 应用入口
├── config.yaml         # 配置文件
├── requirements.txt    # Python 依赖
├── README.md          # 项目说明
└── wsgi.py            # WSGI 应用
```

## 配置

编辑 `config.yaml` 文件来配置应用：

- `host`: 服务器地址
- `port`: 服务器端口
- `debug`: 调试模式
- `template_dir`: 模板目录
- `log_level`: 日志级别
- `cors`: CORS 配置

## 路由管理

### 添加新路由

在 `app.py` 文件中，您可以使用以下方法添加路由：

```python
# 添加 GET 请求路由
app.add_get('/path', handler_function, name='route_name')

# 添加 POST 请求路由
app.add_post('/path', handler_function, name='route_name')

# 添加 PUT 请求路由
app.add_put('/path', handler_function, name='route_name')

# 添加 DELETE 请求路由
app.add_delete('/path', handler_function, name='route_name')

# 添加支持多种 HTTP 方法的路由
app.add_route('/path', handler_function, methods=['GET', 'POST'], name='route_name')
```

### 路由处理函数

路由处理函数接收一个 `request` 参数，返回响应内容：

```python
def handler_function(request):
    # 处理请求
    # ...
    # 返回响应
    return "响应内容"
```

## 中间件

Litefs 提供了多种中间件：

- `LoggingMiddleware` - 请求日志
- `SecurityMiddleware` - 安全头
- `CORSMiddleware` - CORS 支持
- `HealthCheck` - 健康检查

## 部署

### 使用 Gunicorn

```bash
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:9090 wsgi:application
```

### 使用 uWSGI

```bash
pip install uwsgi
uwsgi --http 0.0.0.0:9090 --wsgi-file wsgi.py
```

### 使用 Waitress

```bash
pip install waitress
waitress-serve --port=9090 wsgi:application
```

## 文档

更多文档请参考: https://github.com/leafcoder/litefs

## License

MIT License